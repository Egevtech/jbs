JBS v0.1.0

J Build System
==============
J Build System - система сборки, которая основана на файлах проекта типа .toml.
При конфигурации проекта под каждый исполняемый файл создается своя конфигурация, которая и читается при чтении.

Поддерживаются дерайвы, которые могут быть объявлены в поле variables и вызваны через "^'название_дерайва'". Их можно использовать для строк, например, в компиляторе или линкере, так и в векторе строк - исходниках.


Примеры по использованию вы можете найти в директории examples/

